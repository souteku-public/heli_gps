# 出典表記の対象と文言（正式パッケージ名つき整理）

「**どのデータを、どの正式名称で使っていて、どれに注釈（出典・加工表記）が要るのか**」を
一覧にした文書です。各項目は**一次情報を実際に取得して照合**しており、
根拠となる規約本文は `docs/terms/` にスナップショットとして保存しています。

- 照合日: **2026-09-08**
- 免責: 本書は技術担当による整理であり、法的助言ではありません。最終判断は原文と法務で。

---

## 0. 結論（3行）

1. **国土地理院（GSI）のデータは 1 つだけ直接使用**しています ＝ 市区町村コード表 `muni.json`
   （地理院地図の `muni.js` 由来。**1,919件が完全一致**することを機械照合で確認済み）。
2. 境界データは **国土交通省の「国土数値情報 行政区域データ（N03）」** であり、
   その**原典が国土地理院**（数値地図（国土基本情報））という**間接的な関係**です。
3. **地理院タイル（地図画像）は一切使っていません**。したがって
   測量法第29条・第30条の**承認申請は不要**（かつ「テレビ番組での利用」は
   国土地理院が明示的に申請不要としている用途です）。**必要なのは出典表記のみ**。

---

## 1. 使用しているデータの正式名称と提供元

| # | 同梱ファイル | **正式なパッケージ名／識別子** | 提供元 | 版・基準日 |
|---|---|---|---|---|
| A | `nnn_decoder/data/muni_boundaries.json.gz` | **国土数値情報「行政区域データ」（N03）第3.0版** ／ 識別子 `N03-21_210101` | **国土交通省** 国土数値情報ダウンロードサイト | 令和3（2021）年1月1日時点 |
| B | `nnn_decoder/data/muni.json` | **地理院地図（GSI Maps）の市区町村コード表** `muni.js`（`GSI.MUNI_ARRAY`） | **国土地理院** | 2026-09-08 取得分と1,919件完全一致 |
| C | `nnn_decoder/data/gun.json` | **geolonia/japanese-addresses** から郡名を抽出して生成 | Geolonia | 2026-08-18 生成・923件 |
| D | `fonts/NotoSansJP-VF.ttf` | **Noto Sans JP（可変フォント, wght 100–900）** Version 2.004 | Google Fonts（原作 Adobe / Source Han Sans 系） | 2026-09-07 取得 |

> A の入手経路: 原典（国交省 N03）→ `smartnews-smri/japan-topography`（1%/0.1%簡略化・
> 2021年9月28日に国交省サイトから取得）→ 本プロジェクトで TopoJSON 再圧縮（1,902ジオメトリ）。

### 使用していないもの（重要）

| | 使っていない理由 |
|---|---|
| **地理院タイル**（標準地図・淡色地図・写真等） | 地図スーパーは**同梱のベクタ境界データのみ**で描画。外部タイル取得コードなし |
| **基盤地図情報・電子地形図・数値地図（国土基本情報）そのもの** | 直接ダウンロード・同梱していない（N03 の原典であるだけ） |
| **国土地理院 逆ジオコーダAPI** | コード上は存在（`nnn_decoder/geocode.py` の `API_URL`）が、**`--geo-mode offline` が既定**で呼ばれない。`online`/`auto` は検証用 |
| Google 系サービス全般 | 完全除去済み |

---

## 2. 注釈（出典・加工表記）が必要なもの／不要なもの

| # | データ | 出典表記 | 「加工した旨」の表記 | 根拠 |
|---|---|---|---|---|
| A | 行政区域データ（N03） | **必要** | **必要**（簡略化・再圧縮しているため） | 国土数値情報ダウンロードサイト コンテンツ利用規約 1)ア・イ |
| B | 市区町村コード表（GSI `muni.js`） | **必要** | **必要**（JSON化＝加工にあたる） | 国土地理院コンテンツ利用規約 1)ア・イ |
| C | 郡テーブル（geolonia） | **不要**（CC0-1.0＝権利放棄） | 不要 | CC0-1.0 |
| D | Noto Sans JP | 画面上の表記は**不要** | — | SIL OFL 1.1（`fonts/OFL.txt` の同梱が義務。§4参照） |
| — | japan-topography（加工元） | **不要**（作者が「クレジット不要」と明記） | — | 同リポジトリ README「クレジット」 |
| — | numpy / Pillow / sounddevice | 画面表記は不要。**ライセンス全文の同梱**が義務 | — | BSD / HPND / MIT |

つまり **画面・番組クレジットに出す必要があるのは A と B の2つだけ**です。

### 2-1. 使うべき文言

**画面内（地図スーパーON時）— 実装済み・消去不可**

```
出典:国土数値情報(国土交通省)・国土地理院を加工して作成
```

`gst_heli/map_render.py` の `CREDIT_TEXT`。地図インセット下部に自動描画され、
枠幅に応じて自動縮小するため切れません。オペレーターは消せません。

**番組クレジット等（地図OFF・住所テロップのみの場合）**

```
この番組の位置情報は、「国土数値情報（行政区域データ）」（国土交通省）
および国土地理院ウェブサイトのデータを加工して作成しています。
```

**文書・README での正式表記（最も厳密な形）**

```
出典：「国土数値情報（行政区域データ）」（国土交通省）
      https://nlftp.mlit.go.jp/ksj/gml/datalist/KsjTmplt-N03-v3_0.html （2021-09-28 取得）
      を加工して作成
出典：国土地理院ウェブサイト https://maps.gsi.go.jp/js/muni.js （2026-09-08 取得）
      を加工して作成
```

### 2-2. 出典を「どこに」出すか（国土地理院の指定）

国土地理院は掲出場所を具体的に指定しています（`docs/terms/gsi_2930_meizi.txt`）。

> システムやWebサイトに測量成果を利用する場合、原則、**測量成果が表示画面に
> 表示されている間は、常に見えるように表示画面に明示**してください。
> なお、技術的に困難など、やむを得ない場合には**説明ページや説明書に記載することも可**
> （ソフトウェアであれば readme ファイル…）

本システムでの対応:

| 状態 | 掲出先 |
|---|---|
| 地図スーパー **ON** | **画面内**（地図インセット内に自動描画）＝上位の要件を満たす |
| 地図スーパー **OFF**（住所テロップのみ） | 番組クレジット等 ＋ `README_SETUP.md` / `THIRD_PARTY_NOTICES.md` |

> なお `muni.json` は測量成果（地図・空中写真）ではなく**コンテンツ**なので、
> 上記の「常時画面表示」は厳密には要求されません。より厳しい側に合わせています。

---

## 3. 測量法の承認申請は必要か → **不要**

N03 のメタデータには次の原典表示が付いています（`docs/terms/mlit_n03_v3_0_metadata.txt`）。

> 原典資料（令和3年）: 国土地理院「数値地図（国土基本情報）」
> ※「測量法に基づく国土地理院長承認（複製）**R2JHf 450**」
> 　「本製品を複製する場合には、国土地理院の長の承認を得なければならない。」

この一文だけを読むと申請が要るように見えますが、**同じページの使用許諾条件**が上書きします。

> このデータの使用許諾条件
> **平成30年度以降：適用する利用規約に基づく（オープンデータ）**
> 上記以外：商用可

使用している N03 は**令和3年基準（平成30年度以降）＝オープンデータ**なので、
適用されるのは国土数値情報の利用規約であり、**複製・公衆送信・翻案が出典表記のみで可能**です。

加えて国土地理院自身が、測量成果についても次の用途を**申請不要**としています
（`docs/terms/gsi_2930_index.txt` / `gsi_2930_qa.txt`）。

> デザイン、内部利用、**テレビ番組**、書籍の挿入図などは申請不要！
>
> Q1-8．「テレビ番組で利用」とは？
> **ニュース番組**、ドラマ、動画、オンライン配信（授業）等での内容補足のため、
> 地図等を画面に表示して利用する場合
>
> ※申請不要の場合は、**出典を記載**してご利用ください。

**本システムの用途（ヘリ中継のニュース素材に位置を重畳）はここに該当します。**

> ⚠ 残る留保: N03 ページには「※本データを二次利用する場合には、国土地理院に
> 申請等必要な場合があります」という一般的な注意書きもあります。上記のとおり
> オープンデータ区分＋テレビ番組利用で申請不要と読めますが、**最終確認は法務**、
> 疑義があれば国土地理院「地図の利用手続」窓口へ照会してください。

---

## 4. フォントは放送で使ってよいか → **問題ありません**

同梱フォントのライセンスを、**フォントファイル内部の name テーブルを直接読んで**確認しました。

| name ID | 内容 | 実際の値 |
|---|---|---|
| 1 / 16 | ファミリー名 | `Noto Sans JP` |
| 5 | バージョン | `Version 2.004-H2` |
| 0 | 著作権 | `(c) 2014-2021 Adobe (http://www.adobe.com/), with Reserved Font Name 'Source'.` |
| **13** | **ライセンス** | **`This Font Software is licensed under the SIL Open Font License, Version 1.1.`** |
| 14 | ライセンスURL | `http://scripts.sil.org/OFL` |
| — | 可変軸 | `wght` 100–900（Thin〜Black の9インスタンス） |
| — | グリフ数 | 17,936 |
| — | **OS/2 fsType** | **0 = Installable Embedding（埋め込み制限なし）** |

### 4-1. なぜ放送で使えるのか

**SIL OFL 1.1 が制限しているのは「フォントソフトウェアそのものの再配布」であって、
フォントで組んだ出力物（レンダリング結果）ではありません。** OFL 本文にも
"The requirement for fonts to remain under this license does not apply to any
document created using the Font Software." とあります。したがって

- 放送画面に文字を出す ＝ **制限なし・クレジット不要・許諾不要**
- 商用利用 ＝ **可**
- `fsType = 0` なので、PDF や映像ファイルへの埋め込みも技術的・法的に制限なし

これは**市販フォントや Windows 同梱フォントとの決定的な違い**です。
モリサワ等の市販フォントは放送利用に別途ライセンスが必要な場合があり、
游ゴシック・メイリオ等は**再配布が不可**なので系列局配布ができません。
OFL フォントを同梱しているのは、この2点を同時に解決するためです。

### 4-2. 守るべき OFL の義務（3点だけ）

| # | 義務 | 本プロジェクトの状態 |
|---|---|---|
| 1 | フォントを配布する際は**ライセンス全文を同梱** | ✅ `fonts/OFL.txt`（4,388 bytes）を同梱。削除禁止と各所に明記 |
| 2 | **フォント単体を有償販売しない**（ソフトへの同梱配布は可） | ✅ 無償配布・同梱のみ |
| 3 | **Reserved Font Name を改変版に使わない** | ✅ RFN は `'Source'`。当方はその名称を使わず、フォントも**バイナリ無改変**（ファイル名を `NotoSansJP[wght].ttf` → `NotoSansJP-VF.ttf` に変更しただけ。OFL はファイル名を規制しない） |

### 4-3. 「ふちどり」「太さ変更」は改変にあたるか → **あたりません**

制御UIの「文字の太さ」は可変フォントの `wght` インスタンスを選ぶ操作
（Pillow の `set_variation_by_name`）、縁取りは描画時のストロークで、
いずれも**フォントファイルを書き換えていません**。OFL の「Modified Version」は
フォントソフトウェア自体の改変を指すため、該当しません。

### 4-4. 各局で別のフォントを選んだ場合

制御UIではシステムフォントも選べます。**その場合は当該フォントのEULAが適用され、
放送利用の可否は各局の責任**になります。同梱の Noto Sans JP を使う限り、
**全局同一の見た目**かつライセンス上安全です（`README_SETUP.md` §5）。

---

## 5. 一次情報のスナップショット（`docs/terms/`）

| ファイル | 内容 | 取得日 |
|---|---|---|
| `agreement_01.html` / `.txt` | 国土数値情報ダウンロードサイト コンテンツ利用規約（政府標準利用規約準拠版・令和2年4月1日制定） | 2026-09-07（2026-09-08 に**改定なしを再確認**） |
| `mlit_n03_v3_0_metadata.txt` | 行政区域データ 第3.0版のメタデータ抜粋（原典表示・使用許諾条件） | 2026-09-08 |
| `gsi_content_terms.html` / `.txt` | 国土地理院コンテンツ利用規約（**令和7年11月20日改正**） | 2026-09-08 |
| `pdl_1.0.txt` | 公共データ利用規約（第1.0版）＝ 現行の国土地理院規約が準拠 | 2026-09-08 |
| `gsi_2930_index.txt` | 国土地理院の測量成果の利用手続（測量法29条・30条／申請要否） | 2026-09-08 |
| `gsi_2930_meizi.txt` | 出典の**記載場所**・記載例 | 2026-09-08 |
| `gsi_2930_qa.txt` | 承認申請Q&A（テレビ番組利用・配布範囲） | 2026-09-08 |

### 規約側の変更点（前回整理からの更新）

- **国土地理院**の規約は令和7年11月20日改正で、政府標準利用規約から
  **公共データ利用規約（PDL1.0）** ベースに変わりました。義務の実質
  （出典記載＋加工表記）は同じで、**出典記載例は「出典：国土地理院ウェブサイト（当該ページのURL）」**。
- **国土数値情報**の規約は**変更なし**（令和2年4月1日制定のまま）。
- 懸案だった「行政区域データは商用可か非商用か」は**解決**しました
  → 平成30年度以降は「**適用する利用規約に基づく（オープンデータ）**」区分で、
  非商用区分の制約（複製物の再配布の除外）は**かかりません**。系列局への配布は可能です。

---

## 6. 残る確認事項（法務向け・技術では確定できないもの）

1. 出典の**掲出方法の最終決定**（地図OFF時に番組クレジットで足りるか、局サイト掲載も行うか）
2. N03 ページの「※二次利用時は国土地理院に申請等必要な場合があります」への最終判断
   （§3 のとおり不要と読めるが、局として照会するかは方針次第）
3. `TERMS_OF_USE.md` の `〔　〕` 部分（提供元名・配布範囲・窓口・制定日）の記入

---

## 7. 将来データを差し替えるときの注意

N03 を新しい年次に更新すると、**承認番号（R2JHf 450 等）と基準日が変わります**。
差し替え時は `docs/DATA_PROVENANCE.md` §3 の手順に従い、
新しいメタデータページを `docs/terms/` に保存し、本書 §1 の表を更新してください。

### 出典表記を1つに減らしたい場合（任意）

N03 の属性には市区町村名そのもの（`N03_004` 等）が含まれているため、
`muni.json` を N03 属性から生成し直せば、**国土地理院への出典表記を落とせます**
（国土数値情報のみで完結）。現状は地理院地図の `muni.js` 由来のため2者併記が必要です。
必要になった段階での検討事項として記録しておきます。
